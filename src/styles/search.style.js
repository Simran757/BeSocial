import { StyleSheet } from "react-native";

const search = StyleSheet.create({
    box:{
        borderColor:"grey",
        borderRadius:15,
        borderWidth: 1, 
        padding:10,

    },
});

export default search;