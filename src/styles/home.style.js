import { StyleSheet } from "react-native";

const home = StyleSheet.create({
    container:{
        padding:20,
        paddingBottom:200,
    },
    mainText:{
        fontSize:30,
        fontWeight: "bold",
    },
    postContainer:{
        paddingTop:20,
        paddingBottom:40,
        
    }
});

 export default home;